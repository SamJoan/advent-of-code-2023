def parse_reports(filename):
    fh = open(filename)
    reports = []
    for line in fh:
        report = [int(x) for x in line.strip().split(' ')]
        reports.append(report)

    return reports

reports = parse_reports('input_test.txt')

def calculate_next_digit(report):
    rows = [report]
    while True:
        next_row = []
        previous_row = rows[-1]
        for i, c in enumerate(prev_row):
            if i == 0:
                continue
            else:
                next_row.append(c - rows[

    sys.exit()

for report in reports:
    next_digit = calculate_next_digit(report)
